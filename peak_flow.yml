script:
  - rm spec/dummy/public/packs-test
  - cd spec/dummy; yarn; bundle exec rake api_maker:generate_models; bundle exec rake assets:precompile
  - ls spec/dummy/public/
  - ls spec/dummy/public/packs-test/
  - bundle exec rails db:schema:load
  - bundle exec rspec
  - bundle exec rubocop
  - bundle exec rails_best_practices

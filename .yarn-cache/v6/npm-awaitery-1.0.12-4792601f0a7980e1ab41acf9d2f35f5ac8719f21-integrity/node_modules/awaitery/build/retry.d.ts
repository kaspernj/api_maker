/**
 * Retries a callback until it succeeds or the timeout is reached without arguments.
 * @template T
 * @overload
 * @param {RetryCallback<T>} arg1 - The callback to retry.
 * @param {undefined} [arg2]
 * @returns {Promise<T>} Resolves with the callback result.
 */
export default function retry<T>(arg1: RetryCallback<T>, arg2?: undefined): Promise<T>;
/**
 * Retries a callback until it succeeds or the timeout is reached with arguments.
 * @template T
 * @overload
 * @param {RetryArgs} arg1 - The arguments.
 * @param {RetryCallback<T>} arg2 - The callback to retry.
 * @returns {Promise<T>} Resolves with the callback result.
 */
export default function retry<T>(arg1: RetryArgs, arg2: RetryCallback<T>): Promise<T>;
export type RetryArgs = {
    /**
     * The timeout in milliseconds
     */
    timeout?: number;
    /**
     * The number of tries (default: 3)
     */
    tries?: number;
    /**
     * The wait time in milliseconds between tries (default: 50)
     */
    wait?: number;
    /**
     * The error message when timing out
     */
    timeoutErrorMessage?: string;
};
export type RetryCallback<T> = () => (T | Promise<T>);
//# sourceMappingURL=retry.d.ts.map